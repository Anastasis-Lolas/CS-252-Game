package model.Square;

public class SlideSquare extends Square{
    /**
     * Constructor:Creates slideSquare in which depending of the location of the square has different abilities
     * Post-condition:Creates a slideSquare (via Super() command)
     * @param pos1
     * @param pos2
     */
    public SlideSquare(int pos1,int pos2){
        super(pos1,pos2);
    }
}
