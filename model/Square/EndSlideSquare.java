package model.Square;

public class EndSlideSquare extends SlideSquare{
    /**
     * Constructor:creates a slideSquare that is in the end of the slide
     * We already have the position so we can check when the end of the slide wii be
     * Post-condition:Creates a end SlideSquare which has a circle
     * @param pos1
     * @param pos2
     */
    EndSlideSquare(int pos1,int pos2){
        super(pos1,pos2);
    }
}
